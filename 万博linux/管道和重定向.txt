管道 |：
	命令1 | 命令2
	解释：将命令1的运行结果传递给命令2作为最后一个参数输入并运行，
	两个命令进行数据传递，通过管道文件传递数据
	管道文件是一个允许单独进程之间互相通信的特殊文件类型
输出重定向 >：
	命令 > 文件
	解释：将命令的输出结果重定向输出到文件，
	在默认情况下，命令的运行结果输出到命令行
	命令 >> 文件
	解释：将命令的输出结果追加到文件
输入重定向 <：
	命令 < 文件
	解释：将命令的输入参数重定向到文件输入，
	也就是说文件的内容作为命令的最后一个参数输入