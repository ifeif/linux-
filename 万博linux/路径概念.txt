文件的管理
创建
修改文件
删除文件

目录的管理    目录=文件夹
创建
修改
删除

如何在命令行定位到要管理的文件或文件夹

windows里的文件夹路径是通过反斜杠(\)来区分的
linux是使用正斜杠(/)区分

工作目录：运行当前命令的目录
	tab键补全命令缺少的路径
 
相对路径（需要一个参照：工作目录）

   ./    当前文件夹内，可以被省略掉。一般用来区分全局命令和当前目录的命令（可执行文件）
   ../   上一级文件夹内

简历在我的桌子上

绝对路径（不需要参照工作目录，又称完整路径）

我的简历在东莞信息技术学校16清华万博2班第五列第4桌的抽屉里

例如：
linux:./123/321	有这文件	
linux:321		没有这个文件